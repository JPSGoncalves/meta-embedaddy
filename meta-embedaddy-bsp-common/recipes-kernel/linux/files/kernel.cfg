# Fallback machine fragment, used by any board with no
# files/<machine>/kernel.cfg of its own. Intentionally empty: a board that
# needs nothing beyond kernel-common.cfg is a valid board, and the fetcher
# needs *some* file to resolve file://kernel.cfg to.
